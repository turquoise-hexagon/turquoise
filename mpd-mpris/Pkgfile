# Description : An implementation of the MPRIS protocol for MPD
# URL         : https://github.com/natsukagami/mpd-mpris
# Maintainer  : camille, camille at lzr dot pw
# Depends on  : mpd dbus

name=mpd-mpris
version=0.4.0
release=1
source=("https://github.com/natsukagami/$name/releases/download/v$version/${name}_${version}_Linux_x86_64.tar.gz")

build() {
    install -Dm755 "$name" -t "$PKG/usr/bin"
}
