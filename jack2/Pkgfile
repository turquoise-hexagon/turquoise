# Description : JACK low-latency audio server for multi-processor machines
# URL         : https://jackaudio.org
# Maintainer  : camille, camille at laserbat dot pw
# Depends on  :

name=jack2
version=1.9.14
release=1
source=("https://github.com/jackaudio/$name/archive/v$version.tar.gz")

build() {
    cd "$name-$version"

    ./waf configure \
        --prefix=/usr

    ./waf build -j "${JOBS:-1}"
    ./waf install --destdir="$PKG"
}
