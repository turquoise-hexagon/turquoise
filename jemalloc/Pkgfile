# Description : jemalloc is a general purpose malloc implementation
# URL         : https://jemalloc.net
# Maintainer  : marin, https://github.com/turquoise-hexagon/ports/issues
# Depends on  : 

name=jemalloc
version=5.1.0
release=1
source=("https://github.com/$name/$name/releases/download/$version/$name-$version.tar.bz2")

build ()
{
    cd "$name-$version"

    ./configure --prefix=/usr

    make

    for i in bin include lib_shared lib_static lib_pc; do
        make DESTDIR="$PKG" "install_$i"
    done
}

# vim: ft=sh
