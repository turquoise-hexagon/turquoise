# Description : Rewrite of Crux's pkgmk
# URL         : https://github.com/54sh/pkgmk
# Maintainer  : marin, https://github.com/turquoise-hexagon/ports/issues
# Depends on  :

name=pkgmk-git
version=git
release=1
source=()

build() {
    cd "$PKGMK_SOURCE_DIR"

    if [[ -d $name ]]; then
        cd "$name"
        git pull
    else
        git clone "https://github.com/54sh/${name%-*}" "$name"
        cd "$name"
    fi

    install -Dm755 "${name%-*}" "$PKG/usr/bin/${name%-*}2"
}
